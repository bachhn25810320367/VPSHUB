# VPS-Hub Loop Backlog (cron reads this, updates each run)
Repo: C:/Users/bachhoang/vps-hub, branch main. Stack: static frontend (frontend/index.html Vue3/Tailwind/Chart.js) + Pages Functions backend (functions/api/[[path]].js, Cosmos REST via Web Crypto, no deps) + Go agents on 2 Azure VPS (vps1 Windows Tokyo, vps2 Ubuntu East Asia; Debian deleted).

## DONE (do not redo)
- Remove debian/vps3 everywhere; backend DEFAULT_SERVERS has 2 nodes (vps1, vps2).
- telemetry/latest includes id + vps_id.
- All % displays use toFixed(2).
- File download/preview/list/upload-chunk/delete proxied via same-origin /api/files/* (secret server-side, bind vps_id). Frontend has zero direct `${tunnel_url}/api/files` calls.
- Push method: `git push origin main` CLI hangs on this host -> use GitHub REST API (token in ~/.git-credentials: blobs->tree->commit->patch ref).

## TODO (pick ONE per run, smallest first)
1. agentSecret ref in frontend now unused (only line 2011) -> remove dead ref + localStorage key, verify node --check.
2. Backend proxy error responses are plain text; return JSON {error} consistently for /api/files/*.
3. Upload loop ignores per-chunk HTTP errors silently except throw; surface failed chunk in uploadStatus.message.
4. vps1.hoangngocbach.id.vn DNS NXDOMAIN (vps2 alive) -> backend should return 502 JSON naming the dead tunnel instead of Cloudflare 530 HTML. Needs user to add DNS/CNAME for vps1 tunnel (cannot fix in code).
5. Remove stray binary vps-agent-linux from repo root (untracked) or gitignore it.
6. surface last_seen_seconds_ago in UI (stale node warning when >90s).

## Constraints
- Host 1GB RAM: no heavy builds. Frontend static only. Backend Web Crypto only, no deps. Never commit secrets (COSMOS_KEY, AGENT_SECRET, Master Key).
- Test: node --check backend file; for frontend extract inline <script> to temp .js then node --check.
- Commit + push via REST API. Verify: git ls-remote origin main + curl pages.dev/api/servers head.
