# VPS-Hub Loop Backlog (cron reads this, updates each run)
Repo: C:/Users/bachhoang/vps-hub, branch main. Stack: static frontend (frontend/index.html Vue3/Tailwind/Chart.js) + Pages Functions backend (functions/api/[[path]].js, Cosmos REST via Web Crypto, no deps) + Go agents on 2 Azure VPS (vps1 Windows Tokyo, vps2 Ubuntu East Asia; Debian deleted).

## DONE (do not redo)
- Node cards show last-seen freshness (formatLastSeen + STALE badge when >90s).
- Upload loop surfaces failed chunks: failedChunks array, per-chunk fail message, "Upload incomplete: chunks N failed" summary, no file-list add on partial upload.
- Remove debian/vps3 everywhere; backend DEFAULT_SERVERS has 2 nodes (vps1, vps2).
- telemetry/latest includes id + vps_id.
- All % displays use toFixed(2).
- File download/preview/list/upload-chunk/delete proxied via same-origin /api/files/* (secret server-side, bind vps_id). Frontend has zero direct `${tunnel_url}/api/files` calls.
- Push method: `git push origin main` CLI hangs on this host -> use GitHub REST API (token in ~/.git-credentials: blobs->tree->commit->patch ref).
- /api/files/* proxy errors return JSON {error} consistently (download/list/upload/delete/share-raw).
- Stray root binary vps-agent-linux deleted + gitignored (was untracked, 5.5MB Go build artifact).
- Frontend dead `agentSecret` ref + localStorage key `vps_agent_secret` removed (was exposing AGENT_SECRET literal; now server-side only).
- /api/files/download returns 502 JSON naming dead tunnel (vps_id + tunnelUrl + agent status) instead of Cloudflare 530 HTML when tunnel dead.
- Extended same dead-tunnel 502 JSON to list/upload-chunk/delete/share-raw: !r.ok guards + catch errors name Tunnel ${id} (${tunnelUrl}) with vps_id, so no Cloudflare 530 HTML leaks anywhere in /api/files/*.
- Removed last 2 dead vps3/Debian icon branches from frontend (node cards + server modal); only vps1/vps2 remain.

- /api/files/share/raw catch names dead tunnel (share.tunnel_url + share_id), completing 502-JSON coverage on all /api/files/* (was generic "Error streaming file").
- Purged leftover vps3 references missed by earlier removal: api servers.ts + telemetryLatest.ts + telemetryHistory.ts (types, defaults, baseline curves) + dev-server.js (server entry, history series, exp-3); Pages Functions [[path]].js already clean.

## TODO (pick ONE per run, smallest first)
3. vps1.hoangngocbach.id.vn DNS NXDOMAIN needs user-side DNS/CNAME fix (cannot fix in code); code side done — all /api/files/* return 502 JSON naming dead tunnel.

## Constraints
- Host 1GB RAM: no heavy builds. Frontend static only. Backend Web Crypto only, no deps. Never commit secrets (COSMOS_KEY, AGENT_SECRET, Master Key).
- Test: node --check backend file; for frontend extract inline <script> to temp .js then node --check.
- Commit + push via REST API. Verify: git ls-remote origin main + curl pages.dev/api/servers head.
